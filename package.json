{
  "name": "dumpstream",
  "bin": {
    "dumpstream": "dist/dumpstream"
  },
  "version": "0.0.1",
  "module": "index.ts",
  "type": "module",
  "private": false,
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "bun --compile index.ts --outfile dist/dumpstream",
    "publish": "npm version patch --no-git-tag-version && bun run build && git add package.json bun.lock && git commit -m \"Release $(node -p \"require('./package.json').version\")\" && npm publish"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.6.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "zod": "^3.25.67"
  }
}
